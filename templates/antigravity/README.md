# Antigravity template

Use the MCP management UI to open the raw config editor, then copy selected server entries from `mcp_config.json`.

Template source:
- `~/.agent-stack/templates/antigravity/mcp_config.json`
