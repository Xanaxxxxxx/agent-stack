# Claude Code template

Use Claude Code MCP commands to add these servers at user scope or project scope.

Example:
- claude mcp add openaiDeveloperDocs --scope user --transport http https://developers.openai.com/mcp

For stdio servers, add command, args, and env using the Claude Code MCP CLI or by editing the relevant config file after checking the current Anthropic docs.
